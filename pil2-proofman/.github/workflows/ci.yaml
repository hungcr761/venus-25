name: CI Checks
on:
  push:
    branches: [main, develop]
  pull_request:
jobs:
  cargo-test:
    name: cargo test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Set up Rust Toolchain
        run: curl https://sh.rustup.rs -sSf | sh -s -- -y
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y protobuf-compiler
          sudo apt install -y build-essential libbenchmark-dev libomp-dev libgmp-dev nlohmann-json3-dev nasm libsodium-dev cmake
          sudo apt-get install -y openmpi-bin openmpi-common libopenmpi-dev          

      - name: cargo test
        run: cargo test --workspace
  cargo-check:
    name: cargo check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install Dependencies
        run: sudo apt-get update && sudo apt-get install -y protobuf-compiler
      - name: Set up Rust Toolchain
        run: curl https://sh.rustup.rs -sSf | sh -s -- -y
  cargo-fmt:
    name: cargo fmt
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Rust Toolchain
        run: curl https://sh.rustup.rs -sSf | sh -s -- -y
      - name: cargo fmt
        run: cargo fmt -- --check
  markdown-book:
    name: markdown book
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Rust Toolchain
        run: curl https://sh.rustup.rs -sSf | sh -s -- -y
      - name: install mdbook
        run: cargo install mdbook
      - name: mdbook build
        run: mdbook build
  cargo-fix:
    name: cargo fix
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install Dependencies
        run: sudo apt-get update && sudo apt-get install -y protobuf-compiler libopenmpi-dev openmpi-bin openmpi-common
      - name: Set up Rust Toolchain
        run: curl https://sh.rustup.rs -sSf | sh -s -- -y
      - name: cargo fix --workspace
        run: |
          # Run cargo fix on the project
          cargo fix --workspace --features no_lib_link --allow-dirty --allow-staged

          # Check for local git changes
          if ! git diff --exit-code; then
              echo "There are local changes after running 'cargo fix --workspace' ❌"
              exit 1
          else
              echo "No changes detected after running 'cargo fix --workspace' ✅"
          fi
  cargo-clippy:
    name: cargo clippy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install Dependencies
        run: sudo apt-get update && sudo apt-get install -y protobuf-compiler libopenmpi-dev openmpi-bin openmpi-common
      - name: Set up Rust Toolchain
        run: curl https://sh.rustup.rs -sSf | sh -s -- -y
      - name: Modify stark-prover-lib
        run: sed -i 's/default = \[\]/default = \["no_lib_link"\]/g' ./provers/starks-lib-c/Cargo.toml
      - name: cargo clippy
        run: cargo clippy --workspace --features no_lib_link -- -D warnings
  cargo-audit:
    name: cargo audit
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install Dependencies
        run: sudo apt-get update && sudo apt-get install -y protobuf-compiler
      - name: Set up Rust Toolchain
        run: curl https://sh.rustup.rs -sSf | sh -s -- -y
      - name: install cargo-audit
        run: cargo install cargo-audit
      - name: cargo audit
        run: cargo audit
  test-fibo:
    name: Test Fibonacci Square (with aggregation)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          override: true
          components: rustfmt, clippy

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "18"
      - name: Install npm
        run: npm install -g npm
      - name: Check npm version
        run: npm --version

      - name: Set up Python 3.10
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install z3-solver
        run: |
          python -m pip install z3-solver

      - name: Check out circom
        uses: actions/checkout@v4
        with:
          repository: iden3/circom
          path: circom-repo


      - name: Build and install Circom
        working-directory: circom-repo
        run: |
          cargo build --release --workspace
          cargo install --path circom

      - name: Check out pil2-proofman-js
        uses: actions/checkout@v4
        with:
          repository: 0xPolygonHermez/pil2-proofman-js
          ref: pre-develop-0.16.0
          path: pil2-proofman-js

      - name: Install pil2-proofman-js dependencies
        working-directory: pil2-proofman-js
        run: npm install

      - name: Check out pil2-compiler
        uses: actions/checkout@v4
        with:
          repository: 0xPolygonHermez/pil2-compiler
          ref: develop-0.9.0
          path: pil2-compiler

      - name: Install pil2-compiler dependencies
        working-directory: pil2-compiler
        run: npm install

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y protobuf-compiler
          sudo apt install -y build-essential libbenchmark-dev libomp-dev libgmp-dev nlohmann-json3-dev nasm libsodium-dev cmake
          sudo apt-get install -y openmpi-bin openmpi-common libopenmpi-dev

      - name: Compile PIL Fibonacci Square
        run: |
          node --max-old-space-size=65536 pil2-compiler/src/pil.js ./examples/fibonacci-square/pil/build.pil -I ./pil2-components/lib/std/pil -o ./examples/fibonacci-square/pil/build.pilout -u ./examples/fibonacci-square/build/fixed -O fixed-to-file

      - name: Generate setup Fibonacci Square
        run: |
          node --max-old-space-size=65536 pil2-proofman-js/src/main_setup.js -a ./examples/fibonacci-square/pil/build.pilout -b ./examples/fibonacci-square/build -r -t ./pil2-components/lib/std/pil -u ./examples/fibonacci-square/build/fixed

      - name: Generate PIL Helpers Fibonacci Square
        run: |
          cargo run --bin proofman-cli pil-helpers --pilout ./examples/fibonacci-square/pil/build.pilout --path ./examples/fibonacci-square/src -o

      - name: Cargo build
        run: RUST_BACKTRACE=1 cargo build -v --workspace

      - name: Generate Custom Commits Fixed Fibonacci Square
        run: |
          cargo run --bin proofman-cli gen-custom-commits-fixed --witness-lib ./target/debug/libfibonacci_square.so --proving-key examples/fibonacci-square/build/provingKey/ --custom-commits rom=buffer.bin
      
      - name: Verify constraints Fibonacci Square
        run: |
          cargo run --bin proofman-cli verify-constraints --witness-lib ./target/debug/libfibonacci_square.so --proving-key examples/fibonacci-square/build/provingKey/ --public-inputs examples/fibonacci-square/src/inputs.json --custom-commits rom=buffer.bin

      - name: Generate proof Fibonacci Square
        run: |
          cargo run --bin proofman-cli prove --witness-lib ./target/debug/libfibonacci_square.so --proving-key examples/fibonacci-square/build/provingKey/ --public-inputs examples/fibonacci-square/src/inputs.json --output-dir examples/fibonacci-square/build/proofs --custom-commits rom=buffer.bin -a

      - name: Verify vadcop final proof Fibonacci Square
        run: |
          cargo run --bin proofman-cli verify-stark --proof ./examples/fibonacci-square/build/proofs/vadcop_final_proof.bin --verkey ./examples/fibonacci-square/build/provingKey/build/vadcop_final/vadcop_final.verkey.bin
      
      - name: Clean proof dir
        run: |
          rm -rf examples/fibonacci-square/build/proofs
        
      # - name: Cargo build distributed
      #   run: RUST_BACKTRACE=1 cargo build --features distributed -v

      # - name: Generate proof Fibonacci Square distributed
      #   run: | 
      #     mpirun --bind-to none -np 3 -x OMP_NUM_THREADS=80 target/debug/proofman-cli  prove --witness-lib ./target/debug/libfibonacci_square.so --public-inputs examples/fibonacci-square/src/inputs.json --proving-key examples/fibonacci-square/build/provingKey/ --output-dir examples/fibonacci-square/build/proofs -a

  test-recursive:
    name: Test Recursive
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          override: true
          components: rustfmt, clippy

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "18"
      - name: Install npm
        run: npm install -g npm
      - name: Check npm version
        run: npm --version

      - name: Set up Python 3.10
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install z3-solver
        run: |
          python -m pip install z3-solver

      - name: Check out circom
        uses: actions/checkout@v4
        with:
          repository: iden3/circom
          path: circom-repo


      - name: Build and install Circom
        working-directory: circom-repo
        run: |
          cargo build --release --workspace
          cargo install --path circom

      - name: Check out pil2-proofman-js
        uses: actions/checkout@v4
        with:
          repository: 0xPolygonHermez/pil2-proofman-js
          ref: pre-develop-0.16.0
          path: pil2-proofman-js

      - name: Install pil2-proofman-js dependencies
        working-directory: pil2-proofman-js
        run: npm install

      - name: Check out pil2-compiler
        uses: actions/checkout@v4
        with:
          repository: 0xPolygonHermez/pil2-compiler
          ref: develop-0.9.0
          path: pil2-compiler

      - name: Install pil2-compiler dependencies
        working-directory: pil2-compiler
        run: npm install

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y protobuf-compiler
          sudo apt install -y build-essential libbenchmark-dev libomp-dev libgmp-dev nlohmann-json3-dev nasm libsodium-dev cmake
          sudo apt-get install -y openmpi-bin openmpi-common libopenmpi-dev

      - name: Compile PIL Recursive (Compressor)
        run: |
          node pil2-proofman-js/src/main_setup_recursive.js -b ./examples/test-recursive/build -c ./examples/test-recursive/test.circom -n test -p ./pil2-components/lib/std/pil -t compressor

      - name: Cargo build (Compressor)
        run: RUST_BACKTRACE=1 cargo build -v --workspace
      
      - name: Verify constraints Recursive (Compressor)
        run: |
          cargo run --bin proofman-cli verify-constraints --witness-lib ./target/debug/libtest_recursive.so --proving-key examples/test-recursive/build/provingKey

      - name: Generate proof Recursive (Compressor)
        run: |
          cargo run --bin proofman-cli prove --witness-lib ./target/debug/libtest_recursive.so --proving-key examples/test-recursive/build/provingKey/ --output-dir examples/test-recursive/build/proofs -y

      - name: Compile PIL Recursive (Aggregation)
        run: |
          node pil2-proofman-js/src/main_setup_recursive.js -b ./examples/test-recursive/build -c ./examples/test-recursive/test.circom -n test -p ./pil2-components/lib/std/pil -t aggregation

      - name: Cargo build (Aggregation)
        run: RUST_BACKTRACE=1 cargo build -v --workspace
      
      - name: Verify constraints Recursive (Aggregation)
        run: |
          cargo run --bin proofman-cli verify-constraints --witness-lib ./target/debug/libtest_recursive.so --proving-key examples/test-recursive/build/provingKey

      - name: Generate proof Recursive (Aggregation)
        run: |
          cargo run --bin proofman-cli prove --witness-lib ./target/debug/libtest_recursive.so --proving-key examples/test-recursive/build/provingKey/ --output-dir examples/test-recursive/build/proofs -y 

      - name: Compile PIL Recursive (Final)
        run: |
          node pil2-proofman-js/src/main_setup_recursive.js -b ./examples/test-recursive/build -c ./examples/test-recursive/test.circom -n test -p ./pil2-components/lib/std/pil -t final_vadcop

      - name: Cargo build (Final)
        run: RUST_BACKTRACE=1 cargo build -v --workspace
      
      - name: Verify constraints Recursive (Final)
        run: |
          cargo run --bin proofman-cli verify-constraints --witness-lib ./target/debug/libtest_recursive.so --proving-key examples/test-recursive/build/provingKey

      - name: Generate proof Recursive (Final)
        run: |
          cargo run --bin proofman-cli prove --witness-lib ./target/debug/libtest_recursive.so --proving-key examples/test-recursive/build/provingKey/ --output-dir examples/test-recursive/build/proofs -y 

      - name: Clean proof dir
        run: |
          rm -rf examples/test-recursive/build/proofs
  test-std:
    name: Test STD
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          override: true
          components: rustfmt, clippy

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "18"
      - name: Install npm
        run: npm install -g npm
      - name: Check npm version
        run: npm --version

      - name: Set up Python 3.10
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install z3-solver
        run: |
          python -m pip install z3-solver

      - name: Check out pil2-proofman-js
        uses: actions/checkout@v4
        with:
          repository: 0xPolygonHermez/pil2-proofman-js
          ref: pre-develop-0.16.0
          path: pil2-proofman-js

      - name: Install pil2-proofman-js dependencies
        working-directory: pil2-proofman-js
        run: npm install

      - name: Check out pil2-compiler
        uses: actions/checkout@v4
        with:
          repository: 0xPolygonHermez/pil2-compiler
          ref: develop-0.9.0
          path: pil2-compiler

      - name: Install pil2-compiler dependencies
        working-directory: pil2-compiler
        run: npm install

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y protobuf-compiler
          sudo apt install -y build-essential libbenchmark-dev libomp-dev libgmp-dev nlohmann-json3-dev nasm libsodium-dev cmake
          sudo apt-get install -y openmpi-bin openmpi-common libopenmpi-dev

      - name: Create dir build lookup
        run: |
          mkdir -p ./pil2-components/test/lookup/build/

      - name: Compile PIL Lookup
        run: |
          node pil2-compiler/src/pil.js ./pil2-components/test/lookup/lookup.pil  -I ./pil2-components/lib/std/pil -o ./pil2-components/test/lookup/build/build.pilout

      - name: Generate setup Lookup
        run: |
          node pil2-proofman-js/src/main_setup.js -a ./pil2-components/test/lookup/build/build.pilout -b ./pil2-components/test/lookup/build

      - name: Generate PIL Helpers Lookup
        run: |
          cargo run --bin proofman-cli pil-helpers --pilout ./pil2-components/test/lookup/build/build.pilout --path ./pil2-components/test/lookup/rs/src -o

      - name: Create dir build permutation
        run: |
          mkdir -p ./pil2-components/test/permutation/build/

      - name: Compile PIL permutation
        run: |
          node pil2-compiler/src/pil.js ./pil2-components/test/permutation/permutation.pil  -I ./pil2-components/lib/std/pil -o ./pil2-components/test/permutation/build/build.pilout

      - name: Generate setup permutation
        run: |
          node pil2-proofman-js/src/main_setup.js -a ./pil2-components/test/permutation/build/build.pilout -b ./pil2-components/test/permutation/build

      - name: Generate PIL Helpers permutation
        run: |
          cargo run  --bin proofman-cli pil-helpers --pilout ./pil2-components/test/permutation/build/build.pilout --path ./pil2-components/test/permutation/rs/src -o
        
      - name: Create dir build connection
        run: |
          mkdir -p ./pil2-components/test/connection/build/

      - name: Compile PIL connection
        run: |
          node pil2-compiler/src/pil.js ./pil2-components/test/connection/connection.pil  -I ./pil2-components/lib/std/pil -o ./pil2-components/test/connection/build/build.pilout

      - name: Generate setup connection
        run: |
          node pil2-proofman-js/src/main_setup.js -a ./pil2-components/test/connection/build/build.pilout -b ./pil2-components/test/connection/build

      - name: Generate PIL Helpers connection
        run: |
          cargo run  --bin proofman-cli pil-helpers --pilout ./pil2-components/test/connection/build/build.pilout --path ./pil2-components/test/connection/rs/src -o

      - name: Create dir build diff_buses
        run: |
          mkdir -p ./pil2-components/test/diff_buses/build/

      - name: Compile PIL diff_buses
        run: |
          node pil2-compiler/src/pil.js ./pil2-components/test/diff_buses/diff_buses.pil  -I ./pil2-components/lib/std/pil -o ./pil2-components/test/diff_buses/build/build.pilout

      - name: Generate setup diff_buses
        run: |
          node pil2-proofman-js/src/main_setup.js -a ./pil2-components/test/diff_buses/build/build.pilout -b ./pil2-components/test/diff_buses/build

      - name: Generate PIL Helpers diff_buses
        run: |
          cargo run  --bin proofman-cli pil-helpers --pilout ./pil2-components/test/diff_buses/build/build.pilout --path ./pil2-components/test/diff_buses/rs/src -o

      - name: Create dir build direct_update
        run: |
          mkdir -p ./pil2-components/test/direct_update/build/

      - name: Compile PIL direct_update
        run: |
          node pil2-compiler/src/pil.js ./pil2-components/test/direct_update/direct_update.pil  -I ./pil2-components/lib/std/pil -o ./pil2-components/test/direct_update/build/direct_update.pilout

      - name: Generate setup direct_update
        run: |
          node pil2-proofman-js/src/main_setup.js -a ./pil2-components/test/direct_update/build/direct_update.pilout -b ./pil2-components/test/direct_update/build

      - name: Generate PIL Helpers direct_update
        run: |
          cargo run  --bin proofman-cli pil-helpers --pilout ./pil2-components/test/direct_update/build/direct_update.pilout --path ./pil2-components/test/direct_update/rs/src -o

      - name: Create dir build range-check
        run: |
          mkdir -p ./pil2-components/test/range_check/build/

      - name: Compile PIL range-check
        run: |
          node pil2-compiler/src/pil.js ./pil2-components/test/range_check/build.pil  -I ./pil2-components/lib/std/pil -o ./pil2-components/test/range_check/build/build.pilout

      - name: Generate setup range-check
        run: |
          node pil2-proofman-js/src/main_setup.js -a ./pil2-components/test/range_check/build/build.pilout -b ./pil2-components/test/range_check/build

      - name: Generate PIL Helpers range-check
        run: |
          cargo run  --bin proofman-cli pil-helpers --pilout ./pil2-components/test/range_check/build/build.pilout --path ./pil2-components/test/range_check/rs/src -o

      - name: Create dir build one-instance
        run: |
          mkdir -p ./pil2-components/test/one_instance/build/

      - name: Compile PIL one-instance
        run: |
          node pil2-compiler/src/pil.js ./pil2-components/test/one_instance/one_instance.pil  -I ./pil2-components/lib/std/pil -o ./pil2-components/test/one_instance/build/one_instance.pilout

      - name: Generate setup one-instance
        run: |
          node pil2-proofman-js/src/main_setup.js -a ./pil2-components/test/one_instance/build/one_instance.pilout -b ./pil2-components/test/one_instance/build

      - name: Generate PIL Helpers one-instance
        run: |
          cargo run  --bin proofman-cli pil-helpers --pilout ./pil2-components/test/one_instance/build/one_instance.pilout --path ./pil2-components/test/one_instance/rs/src -o

      - name: Create dir build virtual-tables
        run: |
          mkdir -p ./pil2-components/test/virtual_tables/build/

      - name: Compile PIL virtual-tables
        run: |
          node pil2-compiler/src/pil.js ./pil2-components/test/virtual_tables/virtual_tables.pil  -I ./pil2-components/lib/std/pil -o ./pil2-components/test/virtual_tables/build/build.pilout

      - name: Generate setup virtual-tables
        run: |
          node pil2-proofman-js/src/main_setup.js -a ./pil2-components/test/virtual_tables/build/build.pilout -b ./pil2-components/test/virtual_tables/build

      - name: Generate PIL Helpers virtual-tables
        run: |
          cargo run  --bin proofman-cli pil-helpers --pilout ./pil2-components/test/virtual_tables/build/build.pilout --path ./pil2-components/test/virtual_tables/rs/src -o

      - name: Create dir build simple
        run: |
          mkdir -p ./pil2-components/test/simple/build/

      - name: Compile PIL simple
        run: |
          node pil2-compiler/src/pil.js ./pil2-components/test/simple/simple.pil  -I ./pil2-components/lib/std/pil -o ./pil2-components/test/simple/build/build.pilout

      - name: Generate setup simple
        run: |
          node pil2-proofman-js/src/main_setup.js -a ./pil2-components/test/simple/build/build.pilout -b ./pil2-components/test/simple/build

      - name: Generate PIL Helpers simple
        run: |
          cargo run  --bin proofman-cli pil-helpers --pilout ./pil2-components/test/simple/build/build.pilout --path ./pil2-components/test/simple/rs/src -o

      - name: Cargo build
        run: RUST_BACKTRACE=1 cargo build -v --workspace

      - name: Verify constraints Lookup
        run: |
          cargo run  --bin proofman-cli verify-constraints --witness-lib ./target/debug/liblookup.so --proving-key ./pil2-components/test/lookup/build/provingKey

      - name: Generate proof Lookup
        run: |
          cargo run  --bin proofman-cli prove --witness-lib ./target/debug/liblookup.so --proving-key ./pil2-components/test/lookup/build/provingKey --output-dir ./pil2-components/test/lookup/build -y

      - name: Verify constraints permutation
        run: |
          cargo run  --bin proofman-cli verify-constraints --witness-lib ./target/debug/libpermutation.so --proving-key ./pil2-components/test/permutation/build/provingKey

      - name: Generate proof permutation
        run: |
          cargo run  --bin proofman-cli prove --witness-lib ./target/debug/libpermutation.so --proving-key ./pil2-components/test/permutation/build/provingKey --output-dir ./pil2-components/test/permutation/build -y

      - name: Verify constraints connection
        run: |
          cargo run  --bin proofman-cli verify-constraints --witness-lib ./target/debug/libconnection.so --proving-key ./pil2-components/test/connection/build/provingKey

      - name: Generate proof connection
        run: |
          cargo run  --bin proofman-cli prove --witness-lib ./target/debug/libconnection.so --proving-key ./pil2-components/test/connection/build/provingKey --output-dir ./pil2-components/test/connection/build -y

      - name: Verify constraints direct_update
        run: |
          cargo run  --bin proofman-cli verify-constraints --witness-lib ./target/debug/libdirect_update.so --proving-key ./pil2-components/test/direct_update/build/provingKey

      - name: Generate proof direct_update
        run: |
          cargo run  --bin proofman-cli prove --witness-lib ./target/debug/libdirect_update.so --proving-key ./pil2-components/test/direct_update/build/provingKey --output-dir ./pil2-components/test/direct_update/build -y

      - name: Verify constraints range-check
        run: |
          cargo run  --bin proofman-cli verify-constraints --witness-lib ./target/debug/librange_check.so --proving-key ./pil2-components/test/range_check/build/provingKey

      - name: Generate proof range-check
        run: |
          cargo run  --bin proofman-cli prove --witness-lib ./target/debug/librange_check.so --proving-key ./pil2-components/test/range_check/build/provingKey --output-dir ./pil2-components/test/range_check/build -y

      - name: Verify constraints one-instance
        run: |
          cargo run  --bin proofman-cli verify-constraints --witness-lib ./target/debug/libone_instance.so --proving-key ./pil2-components/test/one_instance/build/provingKey

      - name: Generate proof one-instance
        run: |
          cargo run  --bin proofman-cli prove --witness-lib ./target/debug/libone_instance.so --proving-key ./pil2-components/test/one_instance/build/provingKey --output-dir ./pil2-components/test/one_instance/build -y

      - name: Verify constraints virtual-tables
        run: |
          cargo run  --bin proofman-cli verify-constraints --witness-lib ./target/debug/libvirtual_tables.so --proving-key ./pil2-components/test/virtual_tables/build/provingKey

      - name: Generate proof virtual-tables
        run: |
          cargo run  --bin proofman-cli prove --witness-lib ./target/debug/libvirtual_tables.so --proving-key ./pil2-components/test/virtual_tables/build/provingKey --output-dir ./pil2-components/test/virtual_tables/build -y

      - name: Verify constraints simple
        run: |
          cargo run  --bin proofman-cli verify-constraints --witness-lib ./target/debug/libsimple.so --proving-key ./pil2-components/test/simple/build/provingKey

      - name: Generate proof simple
        run: |
          cargo run  --bin proofman-cli prove --witness-lib ./target/debug/libsimple.so --proving-key ./pil2-components/test/simple/build/provingKey --output-dir ./pil2-components/test/simple/build
       
      # - name: Verify constraints diff_buses
      #   run: |
      #     cargo run  --bin proofman-cli verify-constraints --witness-lib ./target/debug/libdiff_buses.so --proving-key ./pil2-components/test/diff_buses/build/provingKey

      # - name: Generate proof diff_buses
      #   run: |
      #     cargo run  --bin proofman-cli prove --witness-lib ./target/debug/libdiff_buses.so --proving-key ./pil2-components/test/diff_buses/build/provingKey --output-dir ./pil2-components/test/diff_buses/build
