const int K2 = 1000;

function sum2(int a, int b) : int {
    if (defined(K)) println(K, "K visible inside sum2");
    if (defined(K1)) println(K1, "K1 visible inside sum2");
    if (defined(K2)) println(K2, "K2 visible inside sum2");
    if (defined(c)) println(c, "c visible inside sum2");

    return 2*a + b + K + K2; //  + c;
}
